const color = {
  name: "Color",
  label: "Color",
  items: ["all"],
};

const rate = {
  name: "Rate",
  label: "Rate",
  items: ["all"],
};

// eslint-disable-next-line import/no-anonymous-default-export
export default {rate, color};
